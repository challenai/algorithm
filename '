package solution

var intervals [][]int
var rsu, current int
var currentErase map[int]bool

func eraseOverlapIntervals(nums [][]int) int {
	intervals := nums
	currentErase = map[int]bool{}
	rsu = 0
	dfs(0)
	return rsu
}

func dfs(idx int) {
	for i := idx; i < len(); i++ {

	}
}
